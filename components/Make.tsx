import React from 'react';

const Make = () => {
    return (
        <div className='flex h-screen w-screen items-center justify-center pl-14'>
            <span className='text-red-600'>Make</span>
        </div>
    );
};

export default Make;
